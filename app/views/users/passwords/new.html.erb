<div class="container">
  <div class="row justify-content-center">
    <div class="col-xl-10 col-lg-12 col-md-9">
      <div class="card o-hidden border-0 shadow my-5">
        <div class="card-body p-0">
          <!-- Nested Row within Card Body -->
          <div class="row">
            <div class="col-lg-12">
              <div class="p-5">
                <div class="text-center">
                  <h1 class="h4 text-gray-900 mb-2">Quên mật khẩu?</h1>
                  <p class="mb-4">Hãy cho chúng tôi biết địa chỉ email, chúng tôi sẽ gửi hướng dẫn khôi phục mật khẩu cho bạn!</p>
                </div>
                <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "user" }) do |f| %>
                  <div class="form-group">
                    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control form-control-user", placeholder: "Địa chỉ Email" %>
                  </div>

                  <%= f.submit "Gửi hướng dẫn khôi phục mật khẩu", class: 'btn btn-primary btn-user btn-block' %>
                <% end %>
                <hr>
                <div class="text-center">
                  <a class="small" href="/users/sign_up/">Đăng kí tài khoản!</a>
                </div>
                <div class="text-center">
                  <a class="small" href="/users/sign_in/">Đã có tài khoản? Đăng nhập ngay!</a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
